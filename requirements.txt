# SynthBoard Requirements — Validated on Step 0 (2026-03-21)
# Python 3.13.12 | CUDA 13.1 (driver) | RTX 3050 6GB
#
# DO NOT UPGRADE — these versions validated in Step 0
#
# IMPORTANT: pip install -r requirements.txt will NOT install torch correctly.
# You MUST install torch with CUDA first:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
# Then install the rest:
#   pip install -r requirements.txt

# === Core ML (installed via --index-url, not from this file) ===
# torch==2.10.0+cu130
# torchvision==0.25.0+cu130
# torchaudio==2.10.0+cu130

# === Fine-tuning stack ===
unsloth==2026.3.8
unsloth_zoo==2026.3.4
bitsandbytes==0.49.2
peft==0.18.1
trl==0.24.0
transformers==5.3.0
accelerate==1.13.0
datasets==4.3.0
sentencepiece==0.2.1
torchao==0.16.0
triton-windows==3.6.0.post26
xformers==0.0.35
cut-cross-entropy==25.1.1

# === Backend (to be installed in Step 1) ===
# fastapi
# uvicorn
# python-multipart
# aiosqlite
# python-dotenv
# httpx (already installed as dependency)

# === Data & Utils ===
pandas==3.0.1
pyarrow==23.0.1
psutil==7.2.2
pydantic==2.12.5
huggingface_hub==1.7.2
hf_transfer==0.1.9
safetensors==0.7.0

# === Transitive dependencies (pinned for stability) ===
numpy==2.3.5
protobuf==7.34.1
pyyaml==6.0.3
regex==2026.2.28
tokenizers==0.22.2
diffusers==0.37.0
fsspec==2025.9.0
httpx==0.28.1
httpcore==1.0.9
requests==2.32.5
msgspec==0.20.0
nest-asyncio==1.6.0
